if [ "${1}" == "check" ]
then
    unset LD_LIBRARY_PATH &&
        nix flake check
elif [ "${1}" == "commit" ]
then
    unset LD_LIBRARY_PATH &&
        nix flake check &&
        shift &&
        git commit -am "${@}" --allow-empty --allow-empty-message
fi